# ============================================================
# Tesis: Impacto de variables macroeconómicas globales y
# financieras en la valoración bursátil del sector minería
# de cobre en Chile
# Entorno: Python 3.13 + Jupyter
# ============================================================

# Núcleo de datos
pandas>=2.2
numpy>=1.26
pyarrow>=15.0            # Backend para guardar/leer parquet (data/interim, processed)

# Adquisición de datos
yfinance>=0.2.40          # Precios accionarios, VIX, IPSA (Yahoo Finance)
requests>=2.31            # FRED (endpoint CSV) y descargas genéricas (Cochilco, etc.)
openpyxl>=3.1             # Lectura de Excel (BCCh, Cochilco exportan xlsx)
# Nota: NO usar pandas-datareader (0.10 está roto con pandas>=3.0).

# Econometría
statsmodels>=0.14         # ADF/KPSS, VAR/VECM, ARDL, Johansen, Markov
linearmodels>=6.0         # Panel: PanelOLS (FE/RE), Driscoll-Kraay
arch>=7.0                 # Raíces unitarias robustas (CIPS), GARCH

# Visualización
matplotlib>=3.8
seaborn>=0.13

# Utilidades de notebook
jupyter>=1.0
ipykernel>=6.29
